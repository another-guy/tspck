#!/usr/bin/env node

import * as commander from 'commander';

const cmd =
  commander
    // .description(`tspck is a tool for dealing with TypeScript packages.`)
    .option(`ooo`, `OOOOO.`)
    .parse(process.argv);
